A1: ls -l > listing.txt | date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt
A3: ls /nonexistent_dir /etc > output.txt 2>> errors.txt
A4: ls /nonexistent_dir /etc &> combined.txt
A5: I see the files in /etc on terminal, because it is the standard output for the commend and error is redirct to /dev/null. /dev/null is where to throw away unwanted output
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: grep -E 'ERROR' server.log | wc -l
A9: grep -E '^ERROR' server.log > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -r
A11: grep -oE state:[A-Z]+ /data/yelp/yelp_academic_dataset_business.json | sort | uniq -c | sort -nr | head -5
B1: uid=1063(ishigaki-cs30) gid=1063(ishigaki-cs30) groups=1063(ishigaki-cs30),44(video),988(docker),993(render)
B2: -rw-rw-r--, owner allows read write, group allows read, other alows read
B3: default file gets 664
B4: chmod 000 secret.txt | chmod u+rw secret.txt
